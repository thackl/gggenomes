# Check if variables exist in object

Returns TRUE if all variables exists. If `any=TRUE` returns TRUE if at
least one variable exists.

## Usage

``` r
has_vars(x, vars, any = FALSE)
```

## Arguments

- x:

  object

- vars:

  variables to test

- any:

  if TRUE not all but at least one variable has to exists

## Value

TRUE/FALSE
