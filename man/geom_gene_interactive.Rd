% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_gene_interactive.R
\name{geom_gene_interactive}
\alias{geom_gene_interactive}
\title{Draw interactive gene models (experimental)}
\usage{
geom_gene_interactive(mapping = NULL, ...)
}
\arguments{
\item{mapping}{in addition to the aesthetics supported by \code{\link[=geom_gene]{geom_gene()}},
\code{tooltip}, \code{data_id} and \code{onclick} are supported to control the
interactive behavior of the underlying \pkg{ggiraph} elements (see
\code{\link[ggiraph:geom_polygon_interactive]{ggiraph::geom_polygon_interactive()}} for details on their meaning).
These are only effective when the plot is rendered with
\code{\link[ggiraph:girafe]{ggiraph::girafe()}}; other graphics devices silently ignore them and
render the gene models exactly like \code{\link[=geom_gene]{geom_gene()}}. They must be mapped
with \code{\link[ggplot2:aes]{ggplot2::aes()}}; passing them as fixed arguments is not supported.}

\item{...}{all other arguments are passed on to \code{\link[=geom_gene]{geom_gene()}}.}
}
\description{
Experimental variant of \code{\link[=geom_gene]{geom_gene()}} that renders genes as interactive
SVG elements using the \pkg{ggiraph} package, e.g. to show a tooltip on
hover when the plot is rendered with \code{\link[ggiraph:girafe]{ggiraph::girafe()}}. Requires the
\pkg{ggiraph} package to be installed.
}
\examples{
if (requireNamespace("ggiraph", quietly = TRUE)) {
  p <- gggenomes(genes = emale_genes) +
    geom_gene_interactive(aes(fill = name, tooltip = name))
  ggiraph::girafe(ggobj = p)
}
}
